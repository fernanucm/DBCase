package modelo.tools;

/**
 * Tipo enumerado para los dominios de los atributos
*/
public enum TipoDominio {
	BIT,
	BLOB,
	CHAR,
	DATE,
	DATETIME,
	DECIMAL,
	FLOAT,
	INTEGER,
	TEXT,
	TIME,
	VARCHAR
}
